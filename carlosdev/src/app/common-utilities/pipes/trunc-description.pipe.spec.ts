import { TruncDescriptionPipe } from './trunc-description.pipe';

describe('TruncDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
